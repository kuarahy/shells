import React from "react";
import type { FormPageConfig } from "../../types";

/** @todo Not yet implemented */
export function FormPage({ title }: FormPageConfig): React.ReactElement {
  return <div data-shell="form-page">{title} — not yet implemented</div>;
}
