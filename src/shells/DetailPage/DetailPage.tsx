import React from "react";
import type { DetailPageConfig } from "../../types";

/** @todo Not yet implemented */
export function DetailPage({ title }: DetailPageConfig): React.ReactElement {
  return <div data-shell="detail-page">{title} — not yet implemented</div>;
}
